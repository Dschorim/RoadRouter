// utils_local.js
import { APP } from './state.js';

export function isClickOnRoute(latlng) {
    // Basic check if a click is on the route (placeholder for more complex logic if needed)
    // The original Main.js invoked findClosestPointOnPolyline which we might need to export or duplicate
    // For now, let's keep it simple or we can move findClosestPointOnPolyline here too.
    return false; // To be implemented properly if needed, or moved from main.js
}
